"""Module blindtest (Epic 1) : import de playlists publiques et, à terme,
matching/jeu blind-test. Isolé du reste de l'app — sa propre DB SQLite
(`database.py`), ses propres modèles (`models.py`), aucune dépendance vers
`app.database`/`app.models` (AD-7, voir spec-1-1-import-playlist-publique.md).
"""

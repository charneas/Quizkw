fastapi==0.136.3
uvicorn==0.49.0
sqlalchemy==2.0.23
alembic==1.13.1
python-multipart==0.0.32
pydantic==2.13.4
python-dotenv==1.0.0
psycopg2-binary==2.9.9
anthropic==0.107.0
httpx==0.28.1
bcrypt==5.0.0
itsdangerous==2.2.0
# gunicorn : serveur de production réel (DEPLOY.md), Linux uniquement (dépend de fcntl à
# l'exécution) — s'installe sans erreur sur Windows (wheel pur Python) mais ne peut pas y
# être exécuté. Épinglé ici pour que le déploiement Linux installe la version testée.
gunicorn==26.0.0
